# Triage of docs/roadmap.md, 2026-08-15

Session-v53. Every claim in the register was inventoried and verdicted against the repo at
`9a0227a`. 417 claims, ten verdict files under `session-v53/`, each verdict carrying a citation.
Nothing under `src/` was touched; no measurement arm was re-run.

## The instrument check, before the results

A blind pass re-verified a random 10% of the HOLDS verdicts (30 of 295, seed 53) in fresh contexts
given only the original claim text. **3 of 30 disagreed - 10%, under the 1-in-5 threshold the goal
set, so the sweep's output stands.** The three disagreements share one shape: HOLDS granted to a
measurement claim whose backing artifact is gone or tree-deleted. All three were flipped to match
the blind evidence, and the honest reading is that the HOLDS pool leans slightly generous on
artifact-existence claims - the true UNVERIFIABLE count is a few claims higher than any single
pass would report.

Two secondary instrument notes. First, adversarial review of the sweep itself found 2 wrong
verdicts across 417 (C278, C319 - both over-eager DRIFTEDs, both restored) and one batch that read
the forbidden client repo for three claims; that evidence was struck and the breach is written up
in `session-v53/scraps.md`. Second, the claims inventory itself arrived half-written from an
earlier context (150 rows under a header claiming 502) - the register's own census failure mode,
caught by phase 0.

## Headline numbers

| Verdict | Count | Share |
|---|---|---|
| HOLDS | 292 | 70.0% |
| DRIFTED | 48 | 11.5% |
| DEAD | 20 | 4.8% |
| SUPERSEDED | 2 | 0.5% |
| UNVERIFIABLE | 55 | 13.2% |

**Drift rate 30.0%** - roughly the rate the goal's 7-of-15 spot check predicted. The goal's "north
of thirty bad claims" was an undercount by a factor of four.

UNVERIFIABLE by phase: tier 1+2 8.3%, tier 3+4 20.5%, tier 5 + human blocks 12.8%, deferred
fixes + ledgers + rejected 13.0%. Tier 3+4's elevated rate is not skimming: it is the deleted
`session-complxity-research` measurement chain, on which tier 3's items lean hardest. 20 of 46
session artifacts cited by the register are gone from this box, and 14 of those 20 are that one
chain (`session-v53/structural-checks.md` §3b).

## Structural defects in the register itself

1. **Items 53 and 54 are in the index with no section anywhere.** 53's only text hides inside item
   13's amendment; 54 has one index line. (Goal defect 1, confirmed.)
2. **Item 19 is in neither the active list nor the History gap list** - "gaps mean shipped" cannot
   classify it. It survives as one sub-entry line under item 18.
3. **PROVEN is not used as defined.** Defined as "tested"; used as "PROVEN by reading" at least
   three times (C2).
4. **A cited commit hash no longer exists.** `4168d8e` fell to the 2026-08-10 leak-scrub history
   rewrite; its equivalent on today's main is `c228487`. Every bare pre-scrub hash in prose is
   suspect the same way (C267, scraps.md).

## DEAD and SUPERSEDED - work the register carries that is already done or gone

The big four, each a register entry describing work that SHIPPED:

- **Item 26 (tier 1, "fix now") shipped as session-v35 item 3.** The fall-through-to-harvest
  exists at `src/vscode/oracleSurface.ts:3056-3078` with the resolve-before-inject discipline
  kept. A queue entry (old Q18) was pointing a build session at live code. (C29)
- **Item 4 (tier 5) shipped as session-v35 item 2.** `repair.ts:463-503` grants round 2 exactly
  when the error count is falling - the item's own fix shape, measured on a 156-row corpus. (C209)
- **Item 43's build (top of tier 1) shipped in session-v48** - guard, refusal, shrink, channel
  lines all live (`promptBudget.ts`). The entry's own status paragraph concedes this
  mid-paragraph; the headline does not. What remains is only the overflow-frequency measurement.
  (C6-C8)
- **The Go human-decision block's G1 is closed twice over**: `goShapeHooks` is Go's own and is
  WIRED on the pre-fill path (`fnGen.ts:4891`), with the field leg lit and a qualifier-aware
  skipCandidate. Two of the block's three "coupled decisions" describe a resolved state.
  (C273-C276, C148)

Also dead or superseded: the v33-ratification human block (S4 records full ratification and the
file's retirement - C282-C284), item 17's "countable today" premise (the file behind it is
deleted - C171), item 33's invalidation record (its fix artifacts and PRE-HARNESS-FIX copies are
gone - C43-C45), `docs/persona-research.md` "still untracked" (it is tracked - C272), the S40-2
deferred fix (file deleted - C302), the sessions-branch deferred pointer (branch scrubbed - C300),
two rust-ledger rows (both render fixes shipped - C372, C377), the gesture-menu hide-vs-refuse
premise (when-clauses already hide - C335), the Go repair-lang row (shipped v30 - C340), and the
extraction.ts misleading-parallelism comment (already rewritten honest - C312).

## DRIFTED - still real, details wrong

Full corrections live in the verdict files; `session-v53/nonholds-pool.md` pools all 48. The ones
that change what a session would do:

- **Item 21's instruction is inverted** (goal defect 3): the `todo` markers came off 2026-08-10;
  the rows are GREEN today as `KNOWN WRONG:` legs and go RED when the leg lands. Any build session
  needs that stated in its goal or it will revert its own success. (C31)
- **Item 41's premise has moved under it.** `readFnGenConfig` is now class-aware
  (budget-profile cells), and the cloud backends get `FRONTIER_MAX_TOKENS = 64000`, not the local
  2048. "Every number was chosen against the local 30B and now gates the cloud backends
  unchanged" is one build behind - and notably the change has no supersessions entry. 41b is
  substantially mooted; 41a/41c/41d stand. (C46, C47, C53, C328)
- **Item 48's Rust masking claim is wrong in the letter**: the use-path leg is TEST-GEN only, so
  the fn-gen surface carries no import payload in ANY language. The Python finding stands; the
  cross-language framing understates the hole. (C105)
- **Item 22's sweep is 234 citations across 57 files**, not six files (goal defect 4). (C14)
- **Item 23's "fixed" rows drifted twice**: the perf row ships an absolute <5ms bound after the
  10x ratio was tried and failed (the roadmap describes the rejected fix), and both cited line
  numbers are stale. The remaining work (fake clock) stands. (C16-C19)
- **The fimComment deferred fix cites a red row that is neither red nor named C1**: it survives
  retitled `KNOWN WRONG` at `adversarial-v36-p1.test.cjs:362`, GREEN by asserting the defect. The
  fix must flip that row, not write a new oracle. Old queue Q17 carried the wrong instruction.
  (C288, C291)
- **Item 8's cap is 32, not 8** (raised in v21); the busy-box question itself stands. (C220)
- **Item 7 is two-thirds shipped**: the gate covers Go (four languages, not three), and the
  gate-deadline decouple it proposes was built in v18. What stands is the Rust carve-out. (C212,
  C215)
- **Item 19's ask is partially satisfied**: `column80.apiBase` fronts every Ollama call, so remote
  hosts are one setting away; what remains is UX framed around local. (C244)
- **Item 34's leg counts moved** (Go five, C# four with qualified-usage) - its own amendments are
  the drift. (C127)
- **Item 52 shipped as 2.1.0** and its section runs one release behind, including the S16 cap
  restructure (live default root cap 8, `PREFILL_TYPE_CAP` feeds only the `shipped` stop). (C203)

## UNVERIFIABLE - and what would settle each

All 55 rows carry a named check in the verdict files. The pattern: **the register quotes roughly
thirty measurements whose raw artifacts no longer exist on this box.** Recitations survive in
shipped comments and CHANGELOG lines, and per this sweep's standard a recitation is not the
artifact. The clusters:

- The v34/v37 measurement chain (items 30, 34 tables, 17's count, 28's 1-to-3): re-runs named per
  row; `candidates-go-wide.json` (on disk, 7,185 rows) supports a fresh Go ceiling census.
- Item 36/37's S39 scraps: archived on a sessions branch that was scrubbed; recovery or a fresh
  237-row arm.
- Item 2's red run, item 8's spinner numbers, tier-health baseline runs: each needs its quiet-box
  or GPU re-run, none needed tonight.
### Root-cause distribution of the 55

| Root cause | Count | Share |
|---|---|---|
| Session artifact deleted from this box | 34 | 62% |
| Never recorded as an artifact at all | 11 | 20% |
| Scrubbed sessions branch (leak remediation) | 6 | 11% |
| Only evidence lives in the forbidden client repo | 2 | 4% |
| Register referent unnameable (unnamed function; self-citing claim) | 2 | 4% |

40 of 55 were once verifiable and stopped being so through housekeeping (session-dir cleans, the
2026-08-10 branch scrub) - the register kept quoting numbers whose ground was swept from under
them. Six of the deleted 34 survive as recitations in shipped source (`ollama.ts:141-159`,
`config.ts:51-54`); a recitation is not the artifact. The never-recorded 11 are the discipline
finding: prose from day one, recoverable only by re-runs - a capture-or-it-didn't-happen rule at
session close is what prevents the class. The tail four are one-sentence unblocks, already in
queue.md's blocked section.

- Item 13's `{ 0 }` finding (= item 53): the analysed function is UNNAMED in the register, so the
  strongest recent tier-4 finding is currently unactionable as written. Naming it is one line from
  the human. (C226)

## Tier placements that no longer follow from verified content

Named, not re-tiered, per the goal:

- **43** heads tier 1 while its build is shipped; what remains is a measurement.
- **26** sits in tier 1 fully shipped.
- **4** sits in tier 5 fully shipped.
- **33** sits in tier 2 as a record whose artifacts are gone.
- **52** sits in tier 4 released.
- **41** sits at tier 2's head with its 41b sub-item substantially mooted and its central premise
  one build stale.
- **7** sits in tier 5 with two of its three bullets shipped.
- **53** carries tier 4's strongest PROVEN finding and has no section to build from.

---

# RATIFIED 2026-08-16 by the human

Four rulings. Everything under PROPOSED that these do not touch stands as proposed and unratified.

1. **Rewrite roadmap.md from this report, first.** A full rewrite, not a strike-the-dead pass:
   apply the 48 DRIFTED corrections, delete the 20 DEAD, mark each of the 55 UNVERIFIABLE with the
   named check that would settle it, re-tier 43 / 26 / 4 / 33 / 52 / 41 / 7, and give items 53 and
   54 real sections. Delete the two human blocks that are already answered on the record: the v33
   fourteen-row ratification (supersessions S4) and Go's G1 (`goShapeHooks` wired at
   `fnGen.ts:4891`). Nothing else is sequenced from the current file.
2. **Item 22: point the citations at a committed doc.** Move the reasoning worth keeping out of
   the gitignored `session-*/` folders into a committed doc and repoint all 234 cites across 57
   files at it. Delete is the fallback for a citation whose reasoning is not worth moving.
3. **Item 45: build the cold probe, then decide the settle bound.** Do not cut the sleep on the
   warm numbers. Fix the three things that warm a row before it is timed (`assertAlive`'s vetted
   gate cursor, `rootsFrom` perFile=3, `makeOpener`'s run-long `opened` set), get a real cold row,
   and delete or keep the `membersWithSettle` loop on that evidence. Go's gate stays failing at
   about 1.2x until then, and that cost is accepted.
4. **The next build is item 55**, the fn-gen refusal that blames the cursor. Split the three
   branches at `resolveFunctionAtCursor`, name the expected language server per language, add the
   `[fngen] refused:` channel line on all three, and add the language-server extension to the
   requirements list at `docs/user-manual.md:52`. Item 21 stays ahead of the queue but needs its
   scout first.

5. **Items 13/53: the `{ 0 }` function is `trim_out_client_sets`.** No human sentence was needed.
   The case is written up in a committed doc: `docs/dumb-models-work.md:360` holds the stub, `:345`
   the 406 byte fixture arithmetic that makes `0` the truthful expected value in all five
   scenarios, `:325` the qwen suite itself. The rewrite cites that file and item 53 becomes
   actionable. Settle the naming convention in the same pass: that doc names `celeriant_wal`, the
   roadmap says `acme-db`, and both are on main under the 2026-08-12 audit's ruling.
6. **Item 19 is a live lockout, not a UX item, and gets the minimal fail-open.** A non-default
   `column80.apiBase` still walks `resolveTier` against the LOCAL box, so a laptop pointed at a
   company GPU server is told "no usable GPU detected" while the host that would run the model
   sits reachable and idle, and `applyTier` overrides the model with the one local VRAM picked.
   Fix: treat a non-default apiBase as an off-table tier the way `cloud` and `claude-code` already
   are in `buildFnGenService` (`fnGen.ts:1084-1092`) - no hardware probe, no model override,
   `[carve] tier=remote`, fail closed on an unreachable host with a message naming the host. The
   wider self-hosted UX is not bought with it.
7. **Item 7 BUILDS, it is not struck.** The carve-out's origin is `.await` being suppressed, and
   the answer is to handle that edge case, not to leave Rust permanently dark (the comment at
   `completionService.ts:800` calls it permanent today). Rust strikes out invented members like
   the other four languages. Keep the dropped keyword and postfix labels as members that are never
   rendered so the gate's legal list is complete while the prompt still shows only callable ones,
   and handle the `Future` receiver that rewrites all 127 labels as `await.`-prefixed noise.
   Red-before-green in BOTH directions is the ship gate: an invented name suppressed, and `.await`
   still working. The second is the regression that turned this off last time. Note the scout's
   Trap 2 in the goal: the motivating capture had no dot typed, so the gate will not catch that
   exact case.
8. **Item 52 goes to the ledger plus one queue entry.** Strike the section. The command shipped in
   2.1.0 and the yield is 2 names across 30 Rust targets and 5 across 30 TypeScript; whether that
   earns its palette slot is answered by pressing it, so the yield question and Rust's unmeasured
   class-4 enum-variant population move to the Rust dogfood ledger. The compile-graded feature arm
   stays unbuilt and says so: it needs a dictated corpus that does not exist, and C#, Python and
   Go have no backtick population to census, proved by count. One mechanical entry survives:
   **S52-9, make `[RECORD] E` in `adversarial-v37-p1.test.cjs` a ratio** against total
   doc-comment blocks, so the row stops tracking codebase size and re-baselining itself. Item 52's
   open `goal.md` question takes its standing recommendation: the antecedent set for "no new
   claims" is the dictation only, flag rather than fill. It binds no code that exists.

---

# PROPOSED - the human ratifies everything below before roadmap.md changes

## Mechanical versus human-gated, per surviving item

MECHANICAL means all of: fix shape already decided in the register, verdict HOLDS or DRIFTED with
its correction applied, no GPU and no VS Code tier, no measurement arm to design, no ratified
flagship surface, no one-way door. Everything else names its gate.

| Item | Class | Why |
|---|---|---|
| 22 | HUMAN then mechanical | one policy sentence (delete / inline / point-at-doc, recommend point-at-doc), then a one-phase sweep of 234 cites |
| 23 | MECHANICAL | fake clock on the expiry row, then the skip comes off; falsification already written (old Q21) |
| 46 | MECHANICAL | re-cut two rows at the recorded 11-members-1-signed shape, by an agent that did not write v50's bound |
| 51 | MECHANICAL | Python entry in run-arm.cjs; the three existing arms re-run as the regression check |
| 47 | MECHANICAL | run the existing probe, produce Python's per-primitive decomposition; ACTING on it is human |
| Deferred-fix survivors | MECHANICAL | re-derived queue.md is the list; 5 entries died, 10 drifted, corrections applied |
| 21 | HUMAN: design | the leg needs its own scout (per-language resolution legs); fixture and inverted rows are ready and the goal must state the KNOWN WRONG flip |
| 2 | HUMAN: judgment + GPU | "find when it regressed OR rule it fragile" - the OR is the human's; needs the GPU tier |
| 43 remainder | HUMAN: measurement | overflow frequency on real sessions; decide it is worth an instrument at all |
| 41 | HUMAN: measurement design | re-audit first - the budget-profile change moved under it without a supersessions entry; then 41a knee arm (cloud), 41c ladder, 41d latency distribution |
| 42 remainder | HUMAN: design | static C# methods never see their own class; mechanism choice needed |
| 30 | HUMAN: GPU arm | third arm as specified |
| 45 | HUMAN: standing decision | settle-loop bound; now priced - it is the only thing between Go and its gate |
| 48 | HUMAN: design | import payload per language; measurement recipe (same 40 rows) already defined |
| 49 | HUMAN: VS Code tier | the unmeasured TS half needs the extension host |
| 50 | HUMAN: measurement | three languages unmeasured; a bound without a surface-invariance check is the named risk |
| 36, 34, 28, 27, 35, 37 | HUMAN: scout each | the register already says so; verified content does not change it |
| 17, 39, 11, 5, 10 | HUMAN: unscouted ideas | 17 additionally lost its "countable today" premise |
| 6 | HUMAN: arm design | baseline artifacts gone (blind-check flip); the question stands unmeasured |
| 8 | HUMAN: busy-box measurement | probe exists, numbers stale (cap 8 is now 32) |
| 9, 13, 14, 16, 18 | HUMAN: product calls | 13 needs one line first: NAME the `{ 0 }` function |
| 19 | HUMAN: one sentence | apiBase covers the letter; decide whether remote-Ollama UX is wanted or strike it |
| 7 remainder | HUMAN: one sentence | Rust enforcement carve-out is all that is left; keep or strike |
| 52 remainder | HUMAN: re-scope | shipped; fold residue into the register or strike |

### The classification's distribution, in one look

Of the surviving items: 4 are mechanical now (23, 46, 51, 47) plus the 22-entry re-derived queue;
5 convert to mechanical for one human sentence each (22, the 13/53 naming, 19, 7's remainder, 52's
residue); 27 are human-gated - 9 on a scout or design call, 7 on measurement design or a
GPU/VS-Code run, 5 unscouted ideas, 6 genuine judgment calls. So by item count this is a
75% human-gated backlog, by task count the queue still fills an autonomous night, and the
one-sentence column is the cheapest leverage: five sentences roughly double the autonomous-ready
surface. The human backlog is also smaller than the register claims - the v33-rows block is
answered on the record (S4) and the Go block's G1 is shipped code.

## Proposed ordering

1. **Rewrite roadmap.md from this report.** The register is the instrument every session reads,
   and 30% of it is wrong. Nothing else on this list is safe to sequence from the current file.
2. **Item 21** - the worst live product defect (C# renders a false statement the model follows),
   fixture ready, tests pinned. Scout, then build.
3. **Item 22** - one human sentence, then mechanical; ends shipped source pointing at folders a
   clone does not have.
4. **Queue.md survivors** (re-derived alongside this report) - bankable mechanical work, no
   design questions.
5. **Item 47's decomposition run, then the item 45 decision** - the cheapest path to unblocking
   Go's gate, and the decision is already priced.
6. **Item 41 re-audit** - before any knee arm, because its ground moved without a record.
7. **Item 48 scout** - largest measured Python failure family, recipe defined.
8. **Item 42 remainder scout** (static C# receiver), then **tier 3 as ordered** (36 first: the
   53-failures-per-run resolution question, sized with 37).
9. Everything else holds its relative order; the human-decision blocks want a rewrite pass purely
   to delete the closed ones (v33rows, goG1, and the settled halves of the Go block).

One number for the rewrite's honesty: of the register's nine tier-1 "fix now" entries, two are
shipped, one is a measurement wearing a build's clothes, and four more carry drifted instructions.
A fresh clone following tier 1 as written would waste most of its night. That be the cost of a
register nobody re-reads, and it is now paid down.
