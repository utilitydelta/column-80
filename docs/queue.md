# Queue - retired 2026-08-21

The mechanical slice, re-derived by session-v53 from verified claims and drained by session-v55
(24 of its 26 phases shipped; the phase-by-phase ledger with commits is
`session-v55/session-state.md`). `docs/roadmap.md` is the one backlog again; nothing pending lives
here.

Where the survivors went:

- **Q8** (the `some`-to-`every` tightening) -> roadmap **Measurement debt**: one VS Code tier run
  with recorded output, because its quoted precondition has no surviving artifact.
- **Q24** (the cross-host first-run double-run) -> roadmap **Rejected**: WONTFIX, human ruling
  2026-08-21. The riders about `globalState` clobber and the never-asked hazard travel with it.
- **Q23**'s residue (lift the reachability rewrite behind the import hint) -> roadmap **item 56**,
  ratified 2026-08-21.
- **Q26b** -> item 57, **Q26c** -> item 58, **Q3b/Q3c** -> item 59, **Q16b/Q16c** -> item 60.
- **Q1**'s re-file note rides item 61 (the arm rebuild).

Code and test comments citing "queue Q<n>" are provenance, not pointers to pending work; the full
entry text they cite is in git history: `git show e14d6cf:docs/queue.md`.
